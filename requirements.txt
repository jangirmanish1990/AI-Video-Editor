# ── Core backend (installed as needed through the build) ──
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # file uploads
websockets>=12.0
python-dotenv>=1.0

# ── AI / agent ──
openai>=1.30                 # GPT-4o + Whisper
langgraph>=0.2
langchain-openai>=0.1

# ── Video ──
ffmpeg-python>=0.2

# ── Storage (prod) ──
cloudinary>=1.40

# ── Dev / tests ──
pytest>=8.0
flake8>=7.0
httpx>=0.27                  # test client for FastAPI

# ── Optional observability ──
# langsmith>=0.1
